package it.thebertozz.android.housekeeping.objectdetection

/**
Interfaccia che definisce un click listener per la label della Reclycler View
 */
interface CategoryLabelClickListener {
    fun onLabelSelected(label: String)
}